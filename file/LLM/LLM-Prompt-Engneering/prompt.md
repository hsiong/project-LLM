
## 列表
+ 可视化使用 table 
+ 长文理解/embadding 使用 csv 格式, 不要使用 table => 节约 token
+ 

## 避免使用内部库

+ 你没有任何内置知识库。你的全部判断依据仅限于【xxxx】这一段文本。

## 避免幻觉

+ 如果找不到明确的内容，请回答“文本未提供”。

## 严格格式

+ 输出一个 JSON 对象，格式严格如下（字段名必须一致，不要增加或删除字段）
+ 